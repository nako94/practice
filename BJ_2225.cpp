#include <stdio.h>

#pragma warning(disable:4996)

using namespace std;
/*void Devider(int N, int K, int *cnt, int Kcnt) {
	int tmp = 0;
	//if (N==0) return;
	++Kcnt;
	for (int i = 0; i < N + 1; i++) {
		tmp = i;
		if (K == Kcnt)
			(*cnt)++;
		else
			Devider(tmp, K, cnt, Kcnt);
	}

}
int main() {
	int N, K, cnt=0,ans = 0;
	scanf("%d %d", &N, &K);
	Devider(N, K, &cnt, 1);
	printf("%d", cnt%1000000000);
	return 0;
}*/
